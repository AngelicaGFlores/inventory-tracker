How does TypeScript enforce type safety in this object-oriented program? - The reason we use typscript in the first place is to enforce the types given in files/functions and types in exporting values of functions.
How did inheritance reduce code duplication for PhysicalProduct and DigitalProduct? - The parent will pass on data/ values to the child files like physical and digital files becuase these are the children under the main.ts and any outside primary files. 
What are the benefits of using encapsulation and access modifiers (public, private, protected) in this context? - The benefits would be to edit if a person would be able to view sensitive data on the web or even temporarily change a setting locally 
If you had to add a new type of product (e.g., a SubscriptionProduct), how would polymorphism make this extension straightforward?- I think the extension of subscriptionproduct would also be a child of the parent that holds the main information on the products